import 'package:flutter/material.dart';

class MyComplaintDataScreen extends StatelessWidget {
  const MyComplaintDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
